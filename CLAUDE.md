# Claude Code Instructions

Follow `AGENTS.md` in this directory. It is the shared repository instruction file for both Codex and Claude Code.

Before editing:

- Read `AGENTS.md`.
- Check `git status --short`.
- Avoid overwriting changes made by the user or another agent.

When finished, report:

- Files changed.
- Verification commands run.
- Checks skipped and why.
